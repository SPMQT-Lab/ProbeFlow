# ProbeFlow Deep Review — Consolidated Index

**Date written**: 2026-05-28 (review dated 2026-05-27)
**Method**: Six parallel `general-purpose` review agents (physics, numerical stability, image-processing pipeline, IO/instrument format, backend architecture, GUI architecture) deep-reading their assigned subpackages. Two agent batches stalled at the harness level and were re-run; final synthesis below covers all six reports.

## Reports

- [Physics & Math Correctness](2026-05-27-physics.md) — 17 findings (S0=0, S1=4, S2=10, S3=3)
- [Numerical Stability](2026-05-27-numerical.md) — 18 findings (S0=1, S1=8, S2=7, S3=2)
- [Image Processing Pipeline](2026-05-27-image-processing.md) — 16 findings (S0=0, S1=6, S2=8, S3=2)
- [IO & Instrument Format](2026-05-27-io.md) — 22 findings (S0=0, S1=5, S2=12, S3=5)
- [Backend Architecture](2026-05-27-architecture-backend.md) — 18 findings (S0=0, S1=9, S2=7, S3=2)
- [GUI Architecture](2026-05-27-architecture-gui.md) — 23 findings (S0=0, S1=4, S2=17, S3=2)

**Totals**: 114 findings — **S0=1, S1=36, S2=61, S3=16**.

## Executive Summary — Top 10

These are the issues most likely to bite real users or block upcoming work. Each links into the per-angle report for full detail.

| # | Severity | Issue | Source |
|---|---|---|---|
| 1 | **S0** ✅ FIXED | `_polar_decompose` flips both `U` and `Σ` on reflective input, producing a non-positive-semidefinite "stretch" matrix; "preserve image orientation" lattice correction silently applies a reflective transform | [numerical #1](2026-05-27-numerical.md), echoed in [physics #12](2026-05-27-physics.md). **Fix**: `_polar_decompose` no longer flips singular values; S is now always PSD, R inherits the sign of det(T) (improper when reflective), `polar_rotation_deg=NaN` with a `UserWarning` in the reflective case. Also resolves numerical #4 (same root cause). |
| 2 | **S1** ✅ FIXED | `write_sxm_with_planes` has zero overwrite/source-equality protection — a caller passing `out_sxm == src_sxm` silently corrupts the original instrument file | [io #1](2026-05-27-io.md). **Fix**: function now calls `check_overwrite(src, out)` and `check_output_available(out, overwrite=...)` from `probeflow.io.common`, with an `overwrite=False` kwarg. Existing-target raises `FileExistsError`; src==dst raises `ValueError` (resolves symlinks). |
| 3 | **S1** ✅ FIXED | Default ROI sidecar path `with_suffix("").with_suffix(".rois.json")` collapses dotted Createc filenames (`A250320.191933.dat → A250320.rois.json`), losing the timestamp and colliding across every file saved on the same date | [io #2](2026-05-27-io.md). **Fix**: `default_roi_sidecar_path` now uses `path.parent / f"{path.stem}.rois.json"` (`stem` strips only the last extension). `roi_sidecar_candidates` lists the canonical path first; the legacy buggy paths remain as fallbacks so pre-existing sidecars still load. |
| 4 | **S1** ✅ FIXED | `apply_processing_state` never forwards `pixel_size_x_m`/`pixel_size_y_m` to `subtract_background(step_tolerance=True)` or to `facet_level` — every GUI invocation silently interprets `step_threshold_deg=3°` as `0.052 m/pixel` instead of a real surface slope, so step-tolerant background degrades to non-step-tolerant on every Au(111)/Si(111) workflow | [image-proc #1](2026-05-27-image-processing.md). **Fix**: `apply_processing_state` now accepts optional `pixel_size_x_m` / `pixel_size_y_m` kwargs and threads them into both `subtract_background` and `facet_level`. GUI rendering `_apply_processing`, the image-viewer dialog (new `_processing_pixel_sizes_m` helper), the export preflight mixin, and the CLI `pipeline` command all compute calibration from `scan.scan_range_m / shape` and pass it through. |
| 5 | **S1** ✅ FIXED | `apply_processing_state` does not update `scan_range_m` when `scale_image` / `rotate_arbitrary` / `shear` / `affine_lattice_correction` change array shape — PNG scale bars, FFT k-axes, and every downstream physical-unit conversion silently use the old pixel size | [image-proc #4](2026-05-27-image-processing.md). **Fix**: new `apply_processing_state_with_calibration(arr, state, *, scan_range_m)` threads the scan extent through the pipeline. For canvas-expanding ops (`rotate_arbitrary` / `shear` / `affine_lattice_correction`) it preserves pixel size and grows `scan_range_m` with the new shape; for `scale_image` it preserves physical extent and lets `pixel_size = scan_range_m / shape` track the new pixel density. GUI rendering (`_refresh_display_array` now caches `_display_scan_range_m`), the processed-export path (`build_processed_scan_for_export`, `apply_processing_state_to_scan`, viewer mixin), the CLI pipeline command, and the single-op `_apply_to_plane` helper all route through it. |
| 6 | **S1** ✅ FIXED | `quantize_bit_depth` cluster: (a) `vmin`/`vmax` from raw data extrema → one tip-crash pixel destroys precision; (b) runs *before* `arithmetic_ops` in the GUI adapter, so any subsequent constant offset breaks the "256 distinct values" guarantee; (c) data-dependent scale makes the same physical value map to different levels across scans, breaking provenance reproducibility | [physics #3](2026-05-27-physics.md), [numerical #3](2026-05-27-numerical.md), [image-proc #2 + #7](2026-05-27-image-processing.md). **Fix**: `11cdbf6` reworked quantization to record explicit `vmin`/`vmax`/`quantum`, defer GUI quantize steps until after arithmetic, and preserve reproducible scale metadata. |
| 7 | **S1** ✅ FIXED | `fft_magnitude` window/DC cluster: biased mean subtraction with non-rectangular ROI (low-k ring artefact); no window-coherent-gain normalisation feeding into `find_bragg_peaks_in_q_annulus`; `np.hanning` zero-endpoint window; `fourier_filter` / `fft_magnitude` accept different window names | [physics #1 + #5 + #6 + #7](2026-05-27-physics.md), [image-proc #5 + #9](2026-05-27-image-processing.md). **Fix**: `605d072` unified FFT window handling, unbiased masked-ROI DC subtraction, coherent-gain normalization, periodic Hann semantics, and isotropic cutoff behaviour. |
| 8 | **S1** ✅ ALREADY FIXED | Createc `_detect_channel_count` overrides the header's declared `Channels=2` whenever payload trailing diagnostics push past `4·Nx·Ny`, producing two synthetic "backward" planes that are actually instrument telemetry | [io #3](2026-05-27-io.md). **Status**: This was fixed in `5ef77ab Harden Createc DAT reader diagnostics` (May 14 2026, before the review). The current `_detect_channel_count` trusts `header_channels` whenever `payload_float_count >= header_channels * pixels`; the agent appears to have read an older version. Added a regression test `test_header_two_channels_with_extra_telemetry_tail_stays_two` to lock the behaviour in. |
| 9 | **S1** ✅ FIXED | Three competing provenance structures coexist: `ProcessingState` + `ProcessingHistory` (in production), and `ScanGraph` / `ImageNode` / `MeasurementNode` (fully implemented and re-exported as public API but with **zero non-test callers**). Every subpackage `__init__.py` boundary-rules docstring references the graph as canonical, actively misleading contributors. Decide: wire it in or hide it under `_experimental` | [arch-backend #6](2026-05-27-architecture-backend.md). **Fix**: shelved as experimental. Graph symbols (`ScanGraph`, `ImageNode`, `MeasurementNode`, `OpRegistry`, `Node`, `graph_to_dict`, `graph_from_dict`, `materialize_image`) are no longer re-exported from `probeflow.provenance`; callers must import from `probeflow.provenance.graph` directly. `provenance/graph.py` and `provenance/__init__.py` carry explicit "experimental, not production-wired" notes pointing at the canonical `ProcessingHistory` / `ProvenanceStep` records. Boundary-rules docstrings in `core/`, `processing/`, `analysis/`, `io/`, `cli/`, `gui/` `__init__.py` (and `plugins/api.py`, `io/createc_interpretation.py`) now describe the production architecture (Scan → ProcessingState → ProcessingHistory + ExportRecord). New layout test guards against accidental re-promotion. The plugins/ scaffolding (arch-backend #7) is given the same treatment in its `__init__.py`. |
| 10 | **S1** ✅ FIXED | `ImageViewerDialog.closeEvent` only explicitly closes Threshold + STMBackground dialogs. Seven other modeless dialogs (FeatureFinder, ImageInfo, PairCorrelation, FeatureLattice, FFTViewer, PointFFT, LinePeriodicityPlot) stay alive with `WA_DeleteOnClose=False`, holding references back into the viewer — the exact class of bug the `f6aac4a` fix was for. Sibling case also still present in the lattice-grid dock (finding 3 of the GUI report) | [gui-arch #22 + #3](2026-05-27-architecture-gui.md). **Fix**: viewer now owns a `self._modeless_children` registry with `_track_modeless_child(dlg)` / `_close_modeless_children()` helpers. Every modeless show() site (Threshold, STMBackground, FeatureFinder, ImageInfo, PairCorrelation, FeatureLattice, FFTViewer, PointMaskFFT, PeriodicityPlot) calls the tracker; `destroyed` auto-reaps the entry so the list does not grow unbounded. `closeEvent` iterates the registry before `super().closeEvent`, tolerating `RuntimeError` from already-deleted C++ objects. The sibling lattice-grid dock issue (gui-arch #3) was already addressed in `d36acda`. |

## Severity Matrix

| Angle | S0 | S1 | S2 | S3 | Total |
|---|---:|---:|---:|---:|---:|
| Physics | 0 | 4 | 10 | 3 | 17 |
| Numerical | 1 | 8 | 7 | 2 | 18 |
| Image processing | 0 | 6 | 8 | 2 | 16 |
| IO | 0 | 5 | 12 | 5 | 22 |
| Backend arch | 0 | 9 | 7 | 2 | 18 |
| GUI arch | 0 | 4 | 17 | 2 | 23 |
| **Total** | **1** | **36** | **61** | **16** | **114** |

## Themes — issues that recur across angles

### Theme A: Calibration & metadata silently dropped through the pipeline
Multiple angles flag the same root cause — physical-unit metadata is not threaded consistently through processing operations.

- Pixel sizes not forwarded to `subtract_background(step_tolerance)` and `facet_level` ([image-proc #1](2026-05-27-image-processing.md)).
- `scan_range_m` not updated by shape-changing geometric ops ([image-proc #4](2026-05-27-image-processing.md)).
- Forward/backward channel metadata not flipped by `flip_horizontal` → doubly-mirrored backward channel on subsequent blend ([image-proc #11](2026-05-27-image-processing.md)) ✅ FIXED — horizontal flips now swap forward/backward direction tokens in `scan.plane_names`.
- DAT→SXM converter ignores `SCAN_DIR='up'` so round-trip image is upside-down ([io #5](2026-05-27-io.md)) ✅ FIXED — `reconstruct_from_hdr_imgs` now applies `np.flipud` to each plane when `SCAN_DIR='up'`, matching the convention in `write_sxm_with_planes`. Both the standalone DAT→SXM converter and the `write_sxm` DAT-source path benefit. Regression test confirms `ScanYDirec=0` DAT roundtrip preserves display orientation.
- Createc DAT reader's `is_partial_scan` flag and decode warnings discarded by `read_dat` ([io #18](2026-05-27-io.md)) ✅ FIXED — `Scan.warnings` now records DAT decode warnings.
- RHK SM4 `z_units` propagates raw — no `pm`/`Å`/`nm` → SI normalisation ([io #19](2026-05-27-io.md)) ✅ FIXED — recognized length units convert to metres during `read_sm4` scan conversion.

**Recommended fix shape**: make `apply_processing_state` aware of the scan's calibration (or take it as an input dataclass) and update it for every op that changes shape or semantics.

### Theme B: FFT/window normalisation across the codebase ✅ FIXED
Four physics findings plus two image-processing findings cluster around the same window/DC-subtraction pattern. Each is locally small; together they make peak-finding thresholds and FFT magnitudes silently vendor-of-the-window.

- Biased mean subtraction on masked ROI ([physics #1](2026-05-27-physics.md), [image-proc #5](2026-05-27-image-processing.md)) ✅ FIXED
- No window coherent-gain compensation feeding into Bragg peak finding ([physics #5](2026-05-27-physics.md)) ✅ FIXED
- `np.hanning` zero-endpoint vs the periodic Hann ([physics #6](2026-05-27-physics.md)) ✅ FIXED
- `fourier_filter` axis-normalised vs cycles/pixel ([physics #7](2026-05-27-physics.md)) ✅ FIXED
- `fft_magnitude` and `fourier_filter` accept different window vocabularies ([image-proc #9](2026-05-27-image-processing.md)) ✅ FIXED
- `find_bragg_peaks_in_annulus` `==` equality on `local_max_img` ([numerical #5](2026-05-27-numerical.md)) ✅ FIXED

**Fix**: `605d072` unified the window vocabulary, unbiased masked-ROI DC subtraction, coherent-gain normalisation, periodic Hann semantics, and isotropic cutoffs. The plateau-equality Bragg-peak issue was fixed separately in `f758802`.

### Theme C: `quantize_bit_depth` (the new 8/16-bit Image Type feature) ✅ FIXED
Four agents independently flagged this same operation from four different angles. The feature shipped recently (commit `cafb10d`) and exposes a UX promise ("256 distinct levels") that doesn't survive normal pipeline behaviour.

- Data-dependent `vmin`/`vmax` from raw extrema ([physics #3](2026-05-27-physics.md), [numerical #3](2026-05-27-numerical.md)) ✅ FIXED
- Pipeline order puts quantize before `arithmetic_ops` ([image-proc #2](2026-05-27-image-processing.md)) ✅ FIXED
- Quantize runs late so its input shape/extent depends on prior geometric ops ([image-proc #7](2026-05-27-image-processing.md)) ✅ FIXED
- `round`-to-nearest-even bias on idempotent re-quantization ([physics #16](2026-05-27-physics.md)) ✅ FIXED

**Fix**: `11cdbf6` records explicit quantization bounds/quantum and defers GUI quantize steps until after arithmetic so the exported bit-depth guarantee survives the full pipeline.

### Theme D: Re-emerged duplication that Stage 2 was supposed to address ✅ FIXED
Six findings from the backend-architecture angle showed that Stage 2's surgical-seam approach was correct but needed a second pass: two `MeasurementResult` dataclasses, two `FeaturePoint`, two local-maxima kernels, two `roi_statistics` variants, **six** metre→nm/Å formatters, two `ProcessingStep` dataclasses, two geometric-op naming vocabularies, four ROI→mask resolution patterns.

**Fix**: the duplication pass has landed across `4e28ba4`, `ca62f2d`, `2279bdc`, `ade2857`, `319bb7d`, `116a34f`, `0db18b7`, and `dc695f5`: dead duplicate measurement types were removed or turned into adapters/re-exports, shared kernels were introduced under `measurements/`, formatter and ROI-mask helpers were centralised, point rasterisation was shared, and `provenance.ProcessingStep` became `ProvenanceStep` with a compatibility alias.

### Theme E: GUI lifecycle / `RuntimeError` on close
The recent `f6aac4a` fix addressed one site of the C++-object-already-deleted RuntimeError pattern. The GUI agent found sibling cases:

- Seven modeless dialogs not closed by `ImageViewerDialog.closeEvent` ([gui-arch #22](2026-05-27-architecture-gui.md))
- Lattice-grid dock uses `visibilityChanged` instead of `destroyed` for cleanup ([gui-arch #3](2026-05-27-architecture-gui.md))
- `ROIManagerDock` doesn't set its own `objectName` ([gui-arch #5](2026-05-27-architecture-gui.md)) ✅ FIXED — dock now sets `ROIManagerDock` in `__init__`.
- `_open_viewers` weak-keepalive pattern brittle ([gui-arch #13](2026-05-27-architecture-gui.md)) ✅ FIXED — top-level viewers now use a strong-ref set reaped by `destroyed`.

**Recommended fix shape**: collect modeless dialog refs into a `self._modeless_children: list[QDialog]` list, standardise on `WA_DeleteOnClose=True` for non-result dialogs, use `QObject.destroyed` (not `visibilityChanged`) for dock lifecycle hooks.

### Theme F: Silent-failure UX patterns
Across all six reports, the same shape recurs: a function silently degrades (drops a warning, returns a default, falls through a fallback branch) instead of surfacing the issue.

- `_detect_channel_count` overrides header `Channels` silently ([io #3](2026-05-27-io.md))
- Nanonis spec `_parse_header_float` coerces missing tip position to `(0.0, 0.0)` ([io #4](2026-05-27-io.md)) ✅ FIXED — `_parse_header_float` now returns NaN for missing/malformed values. NaN propagates through `spec_position_to_pixel` (returns None) so GUI overlays correctly skip drawing a marker. CLI/spec-viewer formatters print "nan" instead of silently showing the origin.
- `read_all_sxm_planes` truncates plane list to a warning but `Scan` does not record it ([io #10](2026-05-27-io.md)) ✅ FIXED — `read_sxm` records the warning on `Scan.warnings`.
- `gui_adapter.processing_state_from_gui` silently squashes failed step coercions ([image-proc #16](2026-05-27-image-processing.md)) ✅ FIXED — skipped invalid GUI processing steps emit `UserWarning`s.
- `apply_outlier_mask` returns empty arrays when all input is non-finite ([numerical #14](2026-05-27-numerical.md)) ✅ FIXED — all-nonfinite input now warns before returning empty arrays.
- `gmm_autoclip` returns clamped percentiles silently on degenerate data ([numerical #9](2026-05-27-numerical.md))

**Recommended fix shape**: prefer warnings (`warnings.warn(UserWarning)`) attached to `Scan.warnings` or `MeasurementResult.warnings` over silent fallback. Surface them in provenance sidecars and the GUI status bar.

## Architectural posture

The backend layering is genuinely clean — outside `cli/commands/gui.py`, no backend module imports `probeflow.gui` (verified by the architecture agents independently). The Stage 2 seams (`measurements/adapters.py`, `gui/roi_context.py`, `analysis/lattice_correction_workflow.py`) are correctly used by code added since. **The trouble is in the gaps:**

1. The aspirational `ScanGraph` API is fully implemented but unused, and every subpackage boundary docstring promotes it as canonical.
2. The `plugins/` scaffolding (127 LOC, 5 files including an empty `adapters/` subpackage) signals a plugin system that doesn't actually load anything.
3. The new GUI dialogs correctly delegate scientific logic to backend modules, but the floating Feature Counting window (recent `82cd4d8`/`58636e7`) duplicates ~150 LOC of orchestration from `ProbeFlowWindow` and shares signal objects across worker spawns ([gui-arch #1 + #2](2026-05-27-architecture-gui.md)).

The package is at the size where ambiguity about "which seam is canonical" costs real contributor time. None of the architectural issues block users; all are amenable to surgical-seam fixes.

## Suggested order of attack

Sized to roughly Stage-2-style bounded slices. Earlier items unblock or reduce the cost of later items.

1. **Fix the S0 polar-decomposition** ([numerical #1](2026-05-27-numerical.md)). Small change, removes a silent reflective transform from the lattice-correction surface. Includes [numerical #4](2026-05-27-numerical.md) and [physics #12](2026-05-27-physics.md) as dependents.
2. **Silent-data-corruption IO trio**: SXM writer overwrite-source guard ([io #1](2026-05-27-io.md)), ROI sidecar path collapse ([io #2](2026-05-27-io.md)), Createc channel-count override ([io #3](2026-05-27-io.md)). One commit each, all S1, all directly user-affecting.
3. **Thread calibration through `apply_processing_state`** ([image-proc #1 + #4](2026-05-27-image-processing.md)). Single design change ("the function takes a scan-calibration context"); fixes the silent-step-tolerance degradation and the wrong-pixel-size-after-shape-change cluster. Worth doing before any new processing op is added.
4. **Quantize_bit_depth rework** ([physics #3](2026-05-27-physics.md), [numerical #3](2026-05-27-numerical.md), [image-proc #2 + #7](2026-05-27-image-processing.md)) ✅ FIXED in `11cdbf6`.
5. **FFT/window consolidation** ([physics #1 + #5 + #6 + #7](2026-05-27-physics.md), [image-proc #5 + #9](2026-05-27-image-processing.md), [arch-backend #16](2026-05-27-architecture-backend.md)) ✅ FIXED in `605d072`.
6. **GUI dialog-lifecycle audit** ([gui-arch #3 + #22](2026-05-27-architecture-gui.md), plus minor [#5](2026-05-27-architecture-gui.md)). Adopt `_modeless_children` list pattern; standardise `WA_DeleteOnClose=True` for non-result dialogs; switch from `visibilityChanged` to `destroyed`. Closes Theme E.
7. **Stage-2 duplication second pass** ([arch-backend #1–#5 + #10–#12](2026-05-27-architecture-backend.md)) ✅ FIXED across `4e28ba4`, `ca62f2d`, `2279bdc`, `ade2857`, `319bb7d`, `116a34f`, and `0db18b7`.
8. **Decide and document `ScanGraph` and `plugins/`** ([arch-backend #6 + #7](2026-05-27-architecture-backend.md)). Wire-in or hide-under-underscore. Either way, update the boundary-rules docstrings so they describe the actual architecture.
9. **FeatureCounting controller extraction** ([gui-arch #1 + #2](2026-05-27-architecture-gui.md)). Removes the ~150 LOC duplication and fixes the worker-signals race in one go.

## Items consciously *not* re-flagged in this index

- Stage 1 items already addressed and recorded in `docs/review_status.md` (calibrated lattice math, per-axis residuals, lattice correction provenance, pair-correlation context).
- Stage 2 items already addressed (canonical `MeasurementResult` adapter exists; ROI context helper; tool-launch helper; lattice-correction-workflow helper; bounded measurement-row canonicalisation).
- Stage 3 items already resolved (Qt headless test-harness skip; toolbar assets in wheel; CLI surface verification).
- The Python 3.11/3.12 matrix and the GUI-smoke-on-real-Qt caveats from Stage 3 — still open, not in scope for this code-review pass.

## Fix log

Running list of findings closed by the `review/2026-05-27-fixes` branch.
Each row links to the per-angle finding and the commit hash.

| Severity | Finding | Status | Commit |
|---|---|---|---|
| S0 | [numerical #1](2026-05-27-numerical.md) + [physics #12](2026-05-27-physics.md) — `_polar_decompose` reflection | ✅ Fixed | `5bc78ae` |
| S1 | [io #1](2026-05-27-io.md) — `write_sxm_with_planes` overwrite/source guard | ✅ Fixed | `a0b2713` |
| S1 | [io #2](2026-05-27-io.md) — ROI sidecar default path collapses dotted filenames | ✅ Fixed | `e20f135` |
| S1 | [io #3](2026-05-27-io.md) — Createc `_detect_channel_count` overrides header | ✅ Already fixed (regression test added) | `abb63ba` |
| S1 | [io #4](2026-05-27-io.md) — Nanonis spec missing tip position coerced to (0,0) | ✅ Fixed | `e24e060` |
| S1 | [io #5](2026-05-27-io.md) — DAT→SXM `SCAN_DIR='up'` writeback orientation | ✅ Fixed | `20faba6` |
| S1 | [image-proc #1](2026-05-27-image-processing.md) — pixel calibration not forwarded by `apply_processing_state` | ✅ Fixed | `844494f` |
| S1 | [numerical #2](2026-05-27-numerical.md) — `tv_denoise` off-by-one iteration + tol below 50 | ✅ Fixed | `bca914f` |
| S1 | [numerical #6](2026-05-27-numerical.md) / [image-proc #14](2026-05-27-image-processing.md) — `_modal_shift` bin matching diverges from `np.histogram` | ✅ Fixed | `153ffac` |
| S1 | [numerical #9](2026-05-27-numerical.md) — `gmm_autoclip` produces NaN means when `data > median` is empty | ✅ Fixed | `f81b1b7` |
| S1 | [numerical #5](2026-05-27-numerical.md) — `find_bragg_peaks_in_annulus` strict-equality plateau bug | ✅ Fixed | `f758802` |
| S1 | [numerical #7](2026-05-27-numerical.md) — `align_rows(method='linear')` rank-deficient guard + explicit NaN restore | ✅ Fixed | `104e8d4` |
| S1 | [physics #4](2026-05-27-physics.md) — autocorrelation biased estimator → shorter-period bias | ✅ Fixed | `f0b5833` |
| S1 | [numerical #8](2026-05-27-numerical.md) — `_eliminate_profile_jumps` order-dependent forward-only scan | ✅ Fixed | `85d0e0f` |
| S1 | [image-proc #6](2026-05-27-image-processing.md) — `set_zero_plane` ill-conditioned for large images + sub-pixel cluster guard | ✅ Fixed | `bb33e55` |
| S1 | [physics #2](2026-05-27-physics.md) — `rotate_arbitrary` NaN halo from `cval=NaN` + bilinear | ✅ Fixed | `8529c6d` |
| S1 | [gui-arch #2](2026-05-27-architecture-gui.md) — `_FeaturesWorker` shared-signals last-wins race | ✅ Fixed | `09f842a` |
| S1 | [image-proc #3](2026-05-27-image-processing.md) — GUI processing state emitted `align_rows` before background subtraction, so row-median/mean alignment could bias later surface fitting. GUI conversion now emits `plane_bg` / `stm_background` before `align_rows`; explicit hand-authored `ProcessingState` order remains replayable. | ✅ Fixed | `9887a2e` |
| S1×4 | **Quantize bit-depth cluster**: [physics #3](2026-05-27-physics.md) + [numerical #3](2026-05-27-numerical.md) + [image-proc #2](2026-05-27-image-processing.md) + [image-proc #7](2026-05-27-image-processing.md) | ✅ Fixed | `11cdbf6` |
| S1×6 | **FFT/window cluster**: [physics #1](2026-05-27-physics.md) + [physics #5](2026-05-27-physics.md) + [physics #6](2026-05-27-physics.md) + [physics #7](2026-05-27-physics.md) + [image-proc #5](2026-05-27-image-processing.md) + [image-proc #9](2026-05-27-image-processing.md) | ✅ Fixed | `605d072` |
| S2 | [physics #8](2026-05-27-physics.md) — pair-correlation normalization now applies a square-window translational edge-correction approximation when only ROI area is available; raw-count mode remains explicitly marked uncorrected. | ✅ Fixed | (this commit) |
| S2 | [physics #10](2026-05-27-physics.md) — `measure_periodicity` now subtracts DC before windowing, uses periodic Hann windows, reports normalized peak strength, and suppresses conjugate peaks with modulo-safe odd-size indexing. | ✅ Fixed | (this commit) |
| S2 | [physics #11](2026-05-27-physics.md) — line-periodicity FFT no longer presents bin resolution as statistical uncertainty; the FFT bin-width estimate is reported separately in diagnostics/message text. | ✅ Fixed | (this commit) |
| S2 | [numerical #10](2026-05-27-numerical.md) — line-periodicity parabolic peak refinement now clamps invalid/non-finite/out-of-bin deltas instead of applying unstable sub-sample offsets. | ✅ Fixed | (this commit) |
| S2 | [numerical #16](2026-05-27-numerical.md) — lattice feature assignment now uses deterministic half-away-from-zero rounding instead of Python's ties-to-even `round()`. | ✅ Fixed | (this commit) |
| S2 | [arch-backend #12](2026-05-27-architecture-backend.md) — `provenance.ProcessingStep` renamed to `ProvenanceStep` (alias retained) | ✅ Fixed | `0db18b7` |
| S2 | [arch-backend #5](2026-05-27-architecture-backend.md) — 6 metre→display formatters consolidated into `measurements/formatting.py` | ✅ Fixed | `ade2857` |
| S2 | [arch-backend #11](2026-05-27-architecture-backend.md) — 2 ROI→mask helpers consolidated into `measurements/roi_resolve.py` | ✅ Fixed | `319bb7d` |
| S2 | [arch-backend #10](2026-05-27-architecture-backend.md) — point-rasterization inner loop centralised in `measurements/raster.py` | ✅ Fixed | `116a34f` |
| S1 | [arch-backend #1](2026-05-27-architecture-backend.md) — dead `analysis.measurements.MeasurementResult` removed; adapter kept as duck-typed shim | ✅ Fixed | `4e28ba4` |
| S1 | [arch-backend #9](2026-05-27-architecture-backend.md) — `apply_geometric_op_to_scan` accepts both `rot*_cw` and `rotate_*_cw` vocabularies uniformly | ✅ Fixed | `dc695f5` |
| S2 | [io #21](2026-05-27-io.md) — `write_csv` opens output in binary mode with explicit `\n` newlines (no Windows universal-newline translation) | ✅ Fixed | (this commit) |
| S3 | [arch-backend #17](2026-05-27-architecture-backend.md) — stale `cli/commands/__init__.py` migration docstring rewritten to reflect the actual structure | ✅ Fixed | `dc695f5` |
| S2 | [io #6](2026-05-27-io.md) — `io.common.parse_header` Latin-1 fallback (was silently dropping `Å` in `Dacto[Å]z`) | ✅ Fixed | (this commit) |
| S3 | [physics #15](2026-05-27-physics.md) — `apply_outlier_mask` / `robust_scores` docstrings clarify that ``threshold`` is in raw MAD multiples, not σ-equivalents | ✅ Fixed | `f184d50` |
| S1 | [arch-backend #2](2026-05-27-architecture-backend.md) — `FeaturePoint` dataclasses unified; `analysis.feature_finder.FeaturePoint` is now a re-export of the canonical `measurements.models.FeaturePoint` | ✅ Fixed | `ca62f2d` |
| S1 | [arch-backend #4](2026-05-27-architecture-backend.md) — `compute_roi_statistics` is now a thin wrapper around the canonical `measurements.image.roi_statistics` kernel, adding only the `summary` / `area_nm2` GUI extras | ✅ Fixed | `ca62f2d` |
| S1 | Follow-up GUI review — Image Info used NumPy arrays in boolean fallback (`display_arr or raw_arr`) | ✅ Fixed | (this commit) |
| S1 | [gui-arch #3](2026-05-27-architecture-gui.md) follow-up — lattice-grid cleanup now clears active correction previews and uninstalls the viewport event filter | ✅ Fixed | (this commit) |
| S1 | Follow-up IO review — Createc VERT tip positions now use the DAT converter lateral nm/DAC convention for `Dacto[A]xy` | ✅ Fixed | (this commit) |
| S1 | Follow-up IO review — converted DAT→SXM metadata now preserves the Z-CONTROLLER current setpoint | ✅ Fixed | (this commit) |
| S1 | Follow-up IO review — SXM plane orientation now respects per-plane `DATA_INFO` forward/backward direction before falling back to odd/even indices | ✅ Fixed | (this commit) |
| S1 | Follow-up CLI review — batch `dat-sxm` / `dat-png` now return non-zero when any file fails | ✅ Fixed | (this commit) |
| S2 | Follow-up CLI review — `scripts/index_folder.py` imports the canonical `probeflow.core.indexing` module | ✅ Fixed | (this commit) |
| S2 | Follow-up CLI review — `python -m probeflow.cli --help` now works via a package `__main__` entry point | ✅ Fixed | (this commit) |
| S1 | Follow-up GUI export review — processed-image PNG exports now receive provenance and write the expected sidecar | ✅ Fixed | (this commit) |
| S2 | Follow-up GUI review — TV-denoise “load from Browse” now reuses the saved-processing helper used by Feature Counting | ✅ Fixed | (this commit) |
| S1 | Follow-up GUI browse review — thumbnail cards are keyed by entry type and path, avoiding same-stem scan/spectrum collisions | ✅ Fixed | (this commit) |
| S2 | Follow-up numerical review — empty point-mask FFTs now report no dominant frequency instead of a zero-magnitude peak | ✅ Fixed | (this commit) |
| S2 | Follow-up numerical review — coincident pair-correlation points now return a controlled failed/empty result instead of dividing by zero | ✅ Fixed | (this commit) |
| S2 | Follow-up numerical review — all-NaN `measure_periodicity` inputs now return `[]` for CLI-friendly no-result handling | ✅ Fixed | (this commit) |
| S2 | Follow-up spectroscopy review — `numeric_derivative` now validates matching shape and minimum sample count with `ValueError` | ✅ Fixed | (this commit) |
| S1 | [image-proc #4](2026-05-27-image-processing.md) — `apply_processing_state` did not update `scan_range_m` for canvas-expanding ops (rotate_arbitrary / shear / affine_lattice_correction); PNG scale bars, FFT k-axes, and feature pixel→nm conversions silently used the raw scan extent on a now-larger array. New `apply_processing_state_with_calibration` threads scan_range_m through the pipeline; the viewer caches a `_display_scan_range_m` for the scale bar and rulers; processed-export, gui_adapter, CLI pipeline, and CLI single-op paths all route updates back onto the Scan. | ✅ Fixed | `509e5c9` |
| S1 | [arch-backend #6](2026-05-27-architecture-backend.md) + [arch-backend #7](2026-05-27-architecture-backend.md) — `ScanGraph` and `plugins/` had zero production callers but were re-exported as public API while seven boundary-rules docstrings called them canonical, misleading contributors. Shelved as experimental: graph symbols dropped from the `probeflow.provenance` namespace (still importable from `probeflow.provenance.graph`), provenance/__init__ + graph.py + plugins/__init__ + plugins/api.py carry explicit experimental notes, and the boundary-rules docstrings in core/processing/analysis/io/cli/gui/__init__.py rewritten to describe the production Scan → ProcessingState → ProcessingHistory + ExportRecord architecture. Layout test guards against accidental re-promotion. | ✅ Fixed | `3d48c53` |
| S1 | [gui-arch #22](2026-05-27-architecture-gui.md) — `ImageViewerDialog.closeEvent` only explicitly closed Threshold + STMBackground; seven other modeless dialogs (FeatureFinder, ImageInfo, PairCorrelation, FeatureLattice, FFTViewer, PointMaskFFT, PeriodicityPlot) stayed alive holding back-refs into the viewer (the f6aac4a class of bug). New `_modeless_children` registry on the viewer + `_track_modeless_child` / `_close_modeless_children` helpers; every modeless show() site routes through the tracker; `closeEvent` iterates-closes the registry tolerantly of already-deleted C++ objects. | ✅ Fixed | (this commit) |
| S1 | [arch-backend #3](2026-05-27-architecture-backend.md) — `find_image_features` (used by FeatureFinder dialog + ScanFlow previews) and `detect_local_maxima` (used by the measurement dock) had independent NMS loops and a duplicated `_far_enough` helper; a bug fix to one would silently not propagate to the other. New `_detect_peaks_nms` canonical kernel in `measurements/features.py`; both detection functions delegate to it so NMS behaviour stays in sync. New consistency test `test_find_image_features_and_detect_local_maxima_agree_on_peak_locations` locks the contract. FeatureCounting's core segmentation (`segment_particles` / cv2) is unaffected. | ✅ Fixed | `2279bdc` |
| S1 | [gui-arch #1](2026-05-27-architecture-gui.md) — `FeatureCountingWindow` (`gui/features/window.py`) duplicated ~150 LOC of orchestration (`_on_run`, `_on_finished`, `_on_export`, `_on_segment_requested`, `_on_advance_phase2`, `_on_preview`, `_on_mode_changed`, `_on_classify_params_changed`, `_on_mask_*`, `_pct_to_nm2`, classify-summary builder) from `ProbeFlowWindow` (`gui/app.py`). New `FeatureCountingController` (plain QObject, `gui/features/controller.py`) owns all orchestration slots; both hosts pass their panel, sidebar, pool, and `status_bar.showMessage` callback. `window.py` drops from 488 → ~120 LOC; `app.py` drops ~335 LOC. Loading paths stay separate (in-tab Browse vs floating `load_entry`). `_pct_to_nm2` and `_classify_summary` promoted to module-level functions. Tests added for helpers and signal-wiring. | ✅ Fixed | (this commit) |
| S2 | [gui-arch #5](2026-05-27-architecture-gui.md) — `ROIManagerDock` did not set its own `objectName`, making downstream styling/state lookup brittle. | ✅ Fixed | `73bae6a` |
| S2 | [image-proc #11](2026-05-27-image-processing.md) — horizontal flip transformed every plane but left forward/backward labels unchanged, so later direction-aware operations such as forward/backward blending could treat a mirrored backward plane as still-backward. `apply_geometric_op_to_scan(..., "flip_horizontal")` now swaps forward/backward/fwd/bwd tokens in `scan.plane_names` after mirroring. | ✅ Fixed | `cdf04a6` |
| S2 | [io #18](2026-05-27-io.md) — Createc DAT decode warnings, including partial-scan row trimming, were available on `CreatecDatDecodeReport` but discarded by `read_dat`. `Scan` now carries `warnings`, and the DAT reader passes report warnings through for provenance/GUI surfacing. | ✅ Fixed | `cdf04a6` |
| S2 | [io #19](2026-05-27-io.md) — RHK SM4 scan conversion propagated raw length `z_units` instead of normalising pm/nm/µm/Å values to SI. `read_sm4` now converts recognized length units to metres while leaving non-length units such as current `A` unchanged. | ✅ Fixed | `cdf04a6` |
| S2 | [io #10](2026-05-27-io.md) — `read_all_sxm_planes` warned on truncated payloads, but the resulting `Scan` had no record of the warning. `read_sxm` now records caught `UserWarning`s on `scan.warnings`. | ✅ Fixed | `cdf04a6` |
| S2 | [image-proc #16](2026-05-27-image-processing.md) — GUI processing-state conversion silently dropped invalid requested steps (`set_zero_*`, bad geometric specs, malformed arithmetic specs). Invalid step coercions now emit `UserWarning`s while still returning a usable partial `ProcessingState`. | ✅ Fixed | `f3a94a9` |
| S2 | [numerical #14](2026-05-27-numerical.md) — `apply_outlier_mask` returned empty arrays with no signal when every x/y sample was non-finite. It now emits a `UserWarning` before returning the empty filtered arrays and all-false keep mask. | ✅ Fixed | `f3a94a9` |
| S2 | [gui-arch #13](2026-05-27-architecture-gui.md) — top-level `_open_viewers` used a brittle list/removal keepalive pattern for modeless viewer dialogs. `ProbeFlowWindow` now tracks viewers in a strong-ref set and auto-reaps them from the Qt `destroyed` signal, with a finished-signal fallback. | ✅ Fixed | `f3a94a9` |
| S2 | Follow-up GUI robustness — `ImageViewerDialog._refresh_display_array` now tolerates missing `_scan_range_m` in lightweight/test-double construction paths and keeps `_display_scan_range_m=None` rather than raising during error/ROI-paused fallbacks. | ✅ Fixed | `f3a94a9` |

## Process notes

- Two parallel agent batches stalled silently at the harness level. The eventual approach that worked was sequential agents for the last three subpackages (IO, backend architecture, GUI architecture). This is worth knowing for future review passes — for the same-shape work, plan on sequential execution.
- Skeleton-first reports (instructing each agent to write its report file as the first tool call) preserved partial progress when agents stalled. Recommended for any future long-running agent task.
- One agent's completion notification was lost (physics agent — its report exists on disk but no `<task-notification>` arrived). Tracking by JSONL file mtime + report-on-disk presence is the reliable status signal, not the notification channel.
